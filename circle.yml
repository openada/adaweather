machine:
  environment:
    SBT_VERSION: 0.13.12
  java:
    version: openjdk8

dependencies:
  pre:
    - sbt about
    - sbt update
  cache_directories:
    - "~/.ivy2"
    - "~/.sbt"

test:
  override:
    - sbt test